/*
 Next.js Configuration
 Minimal Next.js configuration for the project. This file is intentionally
 lightweight and is the place to add framework-level settings and feature
 flags when required for development or production builds.
*/
/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = nextConfig;
